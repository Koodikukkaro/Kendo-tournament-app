import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#db4744"
    },
    secondary: {
      main: "#9b9484"
    }
  },
  typography: {
    fontSize: 16
  }
});

export default theme;
